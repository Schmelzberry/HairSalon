@{
  Layout = "_Layout";
}

@using HairSalon.Models;

<h1>Clients</h1>

if (@Model.Count == 0)
{
  <h3>No clients have been added to the system!</h3>

}

@foreach (Client client in Model)
{
  <li>client.Name</li>
}